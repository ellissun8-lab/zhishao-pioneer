# 世界行为模型

当前版本是透明规则模型，不是公安预测系统，也不使用真实个人数据。

风险由基础分、敏感区域（`sensitivity × 20`）、三人聚集（`+20`）、风险物品（`+35`）、异常时段（`+10`）和高风险主体乘数（`×1.15`）组成，上限 100。聚集采用 10 分钟半衰期，风险物品采用 30 分钟半衰期。

预测支持 5、10、30 分钟，以活跃事件、当前风险和区域主体数产生可解释的规则概率。What-if 在世界状态深拷贝上运行，四种策略只修改副本。

